# distr-combinators

Combinators for [`rand::distr::Distribution`] objects.

This crate provides tools to compose and transform probability distributions in a functional style, independent of specific data structures.
